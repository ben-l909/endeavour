{
  "addr": "0x100004a20",
  "code": "int __cdecl parse_header(const unsigned char *buf, int len) {\n    if (buf == 0 || len < 8)\n        return -1;\n\n    if (buf[0] != 0xCF || buf[1] != 0xFA)\n        return -2;\n\n    return (int)(buf[6] | (buf[7] << 8));\n}"
}
